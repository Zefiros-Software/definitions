name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: install zpm
      run: npm install -g @zpmpkg/zpm@next
    - name: make test
      run: make test
